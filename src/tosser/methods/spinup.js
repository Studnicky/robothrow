module.exports = async function spinup(targetVelocity) {
	//	So we would make whatever calls we need to make to tell our robotics that they gotta start spinning up here
	//	My assumption is that I would be able to get back some metrics on it's current spin speed via whatever channel
	const hasReachedTargetVelocity = true;
	//	Robot arm goes brrrrr
	return hasReachedTargetVelocity;
};
